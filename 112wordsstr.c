//program to find largest and smallest words in a string
# include <stdio.h>
main()
{
    char str[100];
    int i,l=0;

    printf("Enter a string : ");
    gets(str);

    for(i=0;str[i]!='\0';i++)
    {
        l++;
        if(str[i]==' ')
        {

        }
    }
}
