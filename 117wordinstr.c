// program to check weather the given word in the main string or not
# include <stdio.h>
main()
{
    char str[100],word[20];
    int i;

    printf("Enter a sentence/(main string) : ");
    gets(str);

    printf("Enter a word to check from the main string : ");
    gets(word);

    for(i=0;str[i]!='\0';i++)
    {

    }
}
